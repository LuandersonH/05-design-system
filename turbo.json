{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "dev": {
            "cache": false
        },
        "build": {
            "outputs": [
                "storybook-static/**",
                "dist/**"
            ],
            "dependsOn": [
                "^build"
            ]
        }
    }
}